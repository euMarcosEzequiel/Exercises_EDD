#include <stdio.h>
#define maxElementos 6

main(){

    int numeros[maxElementos];

    numeros[0] = 12;
    numeros[1] = 45;
    numeros[2] = 39;
    numeros[3] = 49;
    numeros[4] = 34;
    numeros[5] = 56;

    printf("Quantidade maxima de elementos: %i", maxElementos);
    printf("\n");
}
